export type LatLng = [ number, number ];

export type Idd = {
    root: string;
    suffixes: string[]
}

export type Flag = {
    png: string;
    svg: string;
    alt: string;
}

export type Side = 'right' | 'left';
